# 2026-04-07

- Home page map section was simplified into a compact "Trip at a glance" strip with one CTA to `/map/`.
- Full custom interactive map module remains on `src/pages/map.astro` only.
- Home page now uses only a tiny static map summary callout (no SVG route, no linked markers).
